# dependencies (paiol is zero-runtime-dep; vendored libs live in vendor/ and ARE committed)
node_modules/

# build output (generated by build.js)
paiol.html
sw.js
dist/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# personal, untracked scratch space (notes, experiments, local files)
personal/

# Claude Code local files (keep private)
CLAUDE.md
CLAUDE.local.md
.claude/

# private planning / notes (keep local)
ROADMAP.md

# generated manual (screenshots + PDF) — shareable but not committed
manual/
